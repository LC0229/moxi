# Moxi - 项目描述与系统架构

## 📝 简单描述

**Moxi** 是一个 AI 驱动的文档自动生成工具。用户只需要：
1. 提供 GitHub 仓库链接
2. Moxi 自动分析代码结构
3. 使用训练好的 LLM 模型生成专业文档（README、架构文档等）
4. 支持 GitHub Actions 自动更新文档

**一句话描述：**
> "Moxi automatically generates and updates documentation for your GitHub repositories using AI."

---

## 🏗️ 系统架构

### 当前架构（MVP）

```
┌─────────────────────────────────────────────────────────┐
│                    User (开发者)                          │
└─────────────────────────────────────────────────────────┘
                        │
                        │ 1. 提供 GitHub 仓库 URL
                        ↓
┌─────────────────────────────────────────────────────────┐
│              CLI / GitHub Actions                        │
│  (本地运行 或 CI/CD 自动触发)                            │
└─────────────────────────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────┐
│          Pipeline 1: Repo Analyzer                       │
│  - 克隆仓库                                              │
│  - 分析项目结构                                          │
│  - 识别关键文件                                          │
└─────────────────────────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────┐
│          Pipeline 4: Doc Generator                       │
│  - 调用训练好的 LLM 模型                                 │
│  - 生成文档                                              │
│  - 格式化输出                                            │
└─────────────────────────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────┐
│              输出文档 (README.md)                         │
│  - 自动提交到仓库                                        │
│  - 或返回给用户                                          │
└─────────────────────────────────────────────────────────┘
```

### 是否有服务器？

**当前版本（MVP）：**
- ❌ **没有独立服务器**
- ✅ **本地运行** 或 **GitHub Actions**（无服务器架构）
- ✅ 用户在自己的环境运行，或通过 CI/CD 自动触发

**未来版本（展示高并发）：**
- ✅ **可选：API 服务器**（FastAPI/Flask）
- ✅ **可选：微服务架构**（Docker/Kubernetes）
- ✅ **任务队列**（Redis/RabbitMQ）

---

## 👤 用户如何使用

### 方式 1: 命令行（CLI）

```bash
# 安装
pip install moxi

# 生成文档
moxi generate https://github.com/user/repo

# 输出：自动生成 README.md
```

### 方式 2: GitHub Actions（自动更新）

```yaml
# .github/workflows/auto-docs.yml
name: Auto Update Docs

on:
  push:
    branches: [main]

jobs:
  generate-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: LC0229/moxi-action@v1
        with:
          repo: ${{ github.repository }}
      # 自动提交更新的文档
```

**用户只需：**
1. 添加 GitHub Actions 配置
2. 每次 push 代码，文档自动更新

### 方式 3: API 服务器（未来）

```bash
# 启动服务器
moxi server --port 8000

# 调用 API
curl -X POST http://localhost:8000/generate \
  -d '{"repo": "https://github.com/user/repo"}'
```

---

## 🚀 如何展示高并发技能

### 1. 批量并发处理（已规划）

**位置：** `dataset_generator/main.py` 增强

```python
# 当前：串行处理
for repo in repos:
    analyze_repository(repo)  # 一个一个处理

# 改进：并发处理
from concurrent.futures import ThreadPoolExecutor

with ThreadPoolExecutor(max_workers=50) as executor:
    futures = [executor.submit(analyze_repository, repo) for repo in repos]
    results = [f.result() for f in futures]

# 性能：100 个仓库从 100 分钟 → 2 分钟
```

**展示点：**
- ✅ 并发处理 100+ 仓库
- ✅ 性能提升 10x
- ✅ 线程池/进程池管理

---

### 2. 异步 I/O（已规划）

**位置：** `crawlers/github_trending.py` 增强

```python
# 当前：同步请求
for repo_url in repos:
    response = requests.get(repo_url)  # 阻塞等待

# 改进：异步请求
import asyncio
import aiohttp

async def fetch_repos(repos):
    async with aiohttp.ClientSession() as session:
        tasks = [fetch_repo(session, repo) for repo in repos]
        results = await asyncio.gather(*tasks)

# 性能：1000 个 API 请求从 1000 秒 → 10 秒
```

**展示点：**
- ✅ 处理 1000+ 并发请求
- ✅ 延迟降低 80%
- ✅ asyncio/await 模式

---

### 3. 任务队列系统（已规划）

**位置：** 新增 `queue/` 模块

```python
# 架构
┌─────────────┐
│  API Server │  → 任务入队
└─────────────┘
       ↓
┌─────────────┐
│ Redis Queue │  → 任务队列
└─────────────┘
       ↓
┌─────────────┐
│   Workers   │  → 并发处理
│ (10 workers)│
└─────────────┘
```

**展示点：**
- ✅ 分布式任务处理
- ✅ 水平扩展（增加 worker）
- ✅ 任务优先级、重试机制

---

### 4. 多级缓存（部分已实现）

**当前：**
- ✅ 本地文件系统缓存（仓库克隆）

**待实现：**
- ⏳ 内存缓存（分析结果）
- ⏳ Redis 缓存（分布式）

```python
# 缓存策略
1. 内存缓存（L1）- 最快
2. Redis 缓存（L2）- 分布式
3. 文件系统缓存（L3）- 持久化
4. 数据库（L4）- 最后
```

**展示点：**
- ✅ 减少数据库负载 90%
- ✅ 缓存失效策略
- ✅ 多级缓存架构

---

### 5. 微服务架构（已规划）

**架构：**
```
┌─────────────────────────────────────────┐
│         API Gateway (Nginx)             │
└─────────────────────────────────────────┘
         │
    ┌────┴────┬──────────┬──────────┐
    ↓         ↓          ↓          ↓
┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│ Repo   │ │Dataset │ │Training│ │ Doc    │
│Analyzer│ │Generator│ │Pipeline│ │Generator│
│Service │ │Service │ │Service │ │Service │
└────────┘ └────────┘ └────────┘ └────────┘
    │         │          │          │
    └─────────┴──────────┴──────────┘
              ↓
        ┌──────────┐
        │  Redis   │
        │ Database │
        └──────────┘
```

**展示点：**
- ✅ 4 个独立服务
- ✅ 服务间通信（REST/消息队列）
- ✅ Docker Compose/Kubernetes 部署

---

## 📊 高并发指标（目标）

| 指标 | 当前 | 目标 | 展示点 |
|------|------|------|--------|
| 并发处理仓库 | 1 | 100+ | 线程池/进程池 |
| API 并发请求 | 1 | 1000+ | asyncio |
| 任务队列容量 | - | 10,000+ | Redis |
| 响应时间 | 10s | <1s | 缓存 |
| 可用性 | - | 99.9% | 容错机制 |

---

## 🎯 简历描述示例

### 高并发相关

1. **"Implemented concurrent repository analysis pipeline processing 100+ repos simultaneously, achieving 10x performance improvement through parallel processing"**

2. **"Built async GitHub API client handling 1000+ concurrent requests, reducing API call latency by 80% using async/await patterns"**

3. **"Designed distributed task queue system using Redis for processing 10,000+ jobs with horizontal scaling capability"**

### 系统设计相关

1. **"Architected microservices-based documentation generation system with 4 independent services, designed service communication patterns using REST APIs and message queues"**

2. **"Implemented multi-tier caching strategy (memory/Redis/filesystem) reducing database load by 90%"**

3. **"Designed scalable database schema for storing 1M+ repository metadata with sub-100ms query performance"**

---

## 📋 实施优先级

| 改进项 | 优先级 | 时间 | 展示价值 |
|--------|--------|------|----------|
| 批量并发处理 | 🔥 高 | 2-3 天 | ⭐⭐⭐⭐⭐ |
| 异步 I/O | 🔥 高 | 2-3 天 | ⭐⭐⭐⭐⭐ |
| 多级缓存 | 🔥 高 | 1-2 天 | ⭐⭐⭐⭐ |
| 任务队列 | 🟡 中 | 3-4 天 | ⭐⭐⭐⭐⭐ |
| 微服务架构 | 🟡 中 | 5-7 天 | ⭐⭐⭐⭐ |

---

## 🔗 相关文件

- `IMPLEMENTATION_ROADMAP.md` - 详细实施计划
- `README.md` - 项目说明
- `src/dataset_generator/main.py` - 主入口（待增强并发）

